# Minimal baseline for local runs
uvicorn>=0.30
pydantic-settings>=2.0
python-dotenv>=1.0
 
# AI Digest Agent dependencies
openai>=1.12.0  # For Azure OpenAI API (compatible with Azure)
duckduckgo-search>=5.0  # For web search (no API key needed)
httpx>=0.26.0  # For async HTTP requests
pydantic>=2.0  # For structured data models